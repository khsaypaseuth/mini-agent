// Shared UI components — populated from Phase 4 onward
// Phase 0: exports design token constants only

export const colors = {
  primary: '#014c8f',
  accent: '#ffa426',
  bg: '#FFFFFF',
} as const;
